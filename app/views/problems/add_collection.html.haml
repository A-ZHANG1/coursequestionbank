
%h4 Create new version of question:
%div.problem_box
  = @problem.html5.html_safe
  %div.additional Created #{@problem.created_date} by #{@problem.instructor.name}

= render :partial => 'add_collection_expand', :locals => {:previous => @problem}
